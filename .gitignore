# IDE 설정
.idea/
*.iml

# OS별 설정
.DS_Store
Thumbs.db

# 빌드 디렉토리
/build/
out/
target/

# Gradle 관련
.gradle/
!gradle/wrapper/gradle-wrapper.jar

# 환경설정
*.env
application.yml
application-*.yml
application.properties
application-*.properties

# 로그, 리포트
*.log
/test-output/

# 컴파일 결과물
*.class
*.jar
*.war

# 프론트 엔드 부분

# node_modules: 패키지 의존성 (용량 큼, 직접 관리 안 함)
node_modules/

# 빌드 결과물 (배포 전용, 개발에 불필요)
dist/
build/

# 환경 변수 (API 키, 비밀번호 등 포함 가능)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE 설정 (VSCode 기준)
.vscode/